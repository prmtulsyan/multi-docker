sudo: required
language: generic

services:
  - docker

before_install:
  - docker build -t prmtulsyan/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run -e CI=true prmtulsyan/react-test npm test

after_success:
  - docker build -t prmtulsyan/multi-client ./client
  - docker build -t prmtulsyan/multi-nginx ./nginx
  - docker build -t prmtulsyan/multi-server ./server
  - docker build -t prmtulsyan/multi-worker ./worker
  # Log in to the docker CLI
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  # Take those images and push them to docker hub
  - docker push prmtulsyan/multi-client
  - docker push prmtulsyan/multi-nginx
  - docker push prmtulsyan/multi-server
  - docker push prmtulsyan/multi-worker
